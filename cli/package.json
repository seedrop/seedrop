{
  "name": "@seedrop/cli",
  "version": "0.2.0-alpha.3",
  "description": "Top-level Seedrop CLI router for id, space, and view commands.",
  "type": "module",
  "bin": {
    "seed": "./bin/seed.mjs",
    "seedrop": "./bin/seed.mjs"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && node -e \"const fs=require('node:fs');const f='dist/cli.js';fs.chmodSync(f,fs.statSync(f).mode|0o755)\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "smoke:install": "node scripts/smoke-install.mjs",
    "prepublishOnly": "npm run build && npm test"
  },
  "files": [
    "bin/",
    "src/",
    "dist/",
    "templates/",
    "clients.json",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "keywords": [
    "llm",
    "ai-agents",
    "cli",
    "agent-coordination",
    "seedrop"
  ],
  "author": "Mariusz Czaykowski",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@seedrop/id": "^0.2.0-alpha.3",
    "@seedrop/space": "^0.2.0-alpha.3",
    "tsx": "^4.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/seedrop/seedrop.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/seedrop/seedrop#readme",
  "bugs": "https://github.com/seedrop/seedrop/issues"
}
