# Committed View Example

This directory mirrors the contents of a repo-local `.seedrop/view/` folder without using that directory name, because Seedrop's own repository ignores live View state.

Use this fixture as the committed proof shape for handoff-ready project orientation: manifest, policy, run journal, continuity packet, task, handoff, and knowledge notes.
